# Paper Quest shared UI

This folder implements the native Godot presentation layer defined by the Paper Quest Figma handoff. Semantic tokens and theme construction are centralized so production workspaces inherit the same Craft, Dark Craft, High Contrast, focus, disabled, and DPI behavior.

The PNG files are low-opacity decorative materials. Reference screenshots are never used as runtime UI.
