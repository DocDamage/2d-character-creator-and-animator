# Manual Verification

The clean-consumer fixture is copied to an isolated project alongside the
runtime addon. A separately launched Godot process loads the generated native
resource and scene, then exercises facing, state setup, two-bone IK, and
equipment swap behavior.
